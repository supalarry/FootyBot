{
    "name": "footy-bot",
    "version": "1.0.0",
    "main": "source/server.ts",
    "repository": "https://github.com/supalarry/FootyBot",
    "author": "Larry <lauris.skraucis@gmail.com>",
    "private": false,
    "license": "UNLICENSED",
    "engines": {
        "node": "^16.x",
        "pnpm": ">= 6.0.0"
    },
    "scripts": {
        "dev": "pnpm dev:database && pnpm dev:server",
        "dev:database": "supabase start",
        "migrate:dev": "doppler run -- pnpm prisma migrate dev",
        "dev:server": "doppler run -- nodemon source/server.ts",
        "build": "rm -rf build && pnpm build:prisma && prettier --write source/ && tsc",
        "build:prisma": "prisma generate",
        "migrate:deploy": "doppler run -- pnpm prisma migrate deploy",
        "start": "doppler run -- node ./build/server.js"
    },
    "devDependencies": {
        "@types/express": "4.17.13",
        "@types/node": "17.0.35",
        "nodemon": "2.0.12",
        "prettier": "2.4.1",
        "prisma": "3.12.0",
        "ts-node": "10.2.1",
        "typescript": "4.4.3"
    },
    "dependencies": {
        "@prisma/client": "3.12.0",
        "express": "4.18.1",
        "ngrok": "4.3.1",
        "telegraf": "4.8.2"
    },
    "packageManager": "pnpm@7.1.0"
}
